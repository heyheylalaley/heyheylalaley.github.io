# Инструкция по настройке Supabase

## ✅ Что уже сделано

1. ✅ Код обновлен для работы с Supabase
2. ✅ Добавлен Supabase JS клиент в index.html
3. ✅ Все API вызовы переведены на Supabase

## 🔧 Что нужно сделать

### 1. Создать таблицы в Supabase (5 минут)

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Скопируйте и выполните SQL из файла `supabase-schema.sql`
5. Проверьте, что таблицы созданы в **Table Editor**

### 2. Настроить Google OAuth в Supabase (10 минут)

1. В Supabase Dashboard перейдите в **Authentication → Providers**
2. Найдите **Google** и включите его
3. Вам понадобятся:
   - **Client ID**: `821999196894-20d8semsbtdp3dcpu4qf2p1h0u4okb39.apps.googleusercontent.com` (уже есть в коде)
   - **Client Secret**: нужно получить из Google Cloud Console

#### Получение Client Secret:

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите проект (или создайте новый)
3. Перейдите в **APIs & Services → Credentials**
4. Найдите OAuth 2.0 Client ID (тот же, что используется в коде)
5. Нажмите на него и скопируйте **Client Secret**
6. Вставьте в Supabase Dashboard в поле **Client Secret**

#### Настройка Redirect URL в Google Cloud Console:

1. В Google Cloud Console в настройках OAuth Client ID
2. Добавьте в **Authorized redirect URIs**:
   ```
   https://qwtwezxoodqfmdqpzkkl.supabase.co/auth/v1/callback
   ```
3. Также добавьте ваш домен GitHub Pages (если используете):
   ```
   https://YOUR_USERNAME.github.io
   ```

### 3. Проверить настройки проекта

В Supabase Dashboard → Settings → API убедитесь, что:
- **Project URL**: `https://qwtwezxoodqfmdqpzkkl.supabase.co` ✅
- **anon public key**: `sb_publishable_iW0DJWq84mfMA30kA_HDOg_Fx99JPKU` ✅

### 4. Добавить начальных пользователей

После создания таблиц, добавьте хотя бы одного пользователя:

1. Перейдите в **Table Editor → users**
2. Нажмите **Insert → Insert row**
3. Добавьте пользователя:
   - **name**: Admin
   - **email**: ваш email (тот же, что используется для Google)
   - **role**: admin

**Важно**: Email должен совпадать с email вашего Google аккаунта!

### 5. Протестировать

1. Откройте приложение
2. Нажмите "Sign in with Google"
3. Авторизуйтесь через Google
4. Проверьте, что вы вошли в систему

## 🔍 Проверка работы

После настройки проверьте:

- ✅ Авторизация через Google работает
- ✅ Загрузка данных работает
- ✅ Добавление записей работает
- ✅ Удаление записей работает
- ✅ Экспорт работает

## ⚠️ Важные замечания

1. **RLS политики**: Убедитесь, что Row Level Security настроен правильно в SQL схеме
2. **Foreign Keys**: Проверьте, что foreign key между logs и users работает
3. **Автоматическое создание пользователей**: Триггер в схеме автоматически создает пользователя в таблице `users` при первой авторизации через OAuth

## 🐛 Решение проблем

### Ошибка "User not found in database"
- Убедитесь, что пользователь добавлен в таблицу `users`
- Или дождитесь автоматического создания через триггер при первой авторизации

### Ошибка "Supabase client not initialized"
- Проверьте, что Supabase скрипт загружен в index.html
- Проверьте консоль браузера на ошибки

### Ошибка авторизации OAuth
- Проверьте, что Google OAuth настроен в Supabase Dashboard
- Проверьте, что Redirect URI добавлен в Google Cloud Console
- Проверьте, что Client Secret правильный

### Ошибки при работе с данными
- Проверьте RLS политики в Supabase Dashboard → Authentication → Policies
- Убедитесь, что пользователь авторизован (проверьте сессию)

## 📞 Поддержка

Если что-то не работает:
1. Проверьте консоль браузера (F12)
2. Проверьте логи в Supabase Dashboard → Logs
3. Убедитесь, что все шаги выполнены

