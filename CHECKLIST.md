# Чеклист настройки проекта

## ✅ Проверено

### Frontend (GitHub Pages)
- [x] `index.html` - создан и настроен
- [x] `styles.css` - создан
- [x] `app.js` - создан
- [x] `CONFIG.GAS_API_URL` - обновлён на новый URL: `AKfycbwPN7R5be2PX35bbtPT8800UbkaYVo86UVJF9v_2qI2xUZrw1vMOCCWyedXB7L7jUFY`
- [x] `CONFIG.GOOGLE_CLIENT_ID` - указан: `821999196894-20d8semsbtdp3dcpu4qf2p1h0u4okb39.apps.googleusercontent.com`

## ⚠️ Требует проверки

### Google Apps Script
- [ ] **SPREADSHEET_ID** - должен быть настроен в Google Apps Script проекте
  - Откройте ваш проект Google Apps Script
  - Найдите строку: `const SPREADSHEET_ID = 'YOUR_SPREADSHEET_ID_HERE';`
  - Замените на ID вашей Google Таблицы
  - ID можно найти в URL таблицы: `https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit`

- [ ] **Структура таблиц** - должны быть созданы листы:
  - [ ] Лист "Users" с колонками: id, name, email, role
  - [ ] Лист "Logs" с колонками: id, userEmail, date, type, factHours, creditedHours, comment, createdAt
  - [ ] Лист "Settings" с колонками: key, value
  
  **Как создать автоматически:**
  1. В Google Apps Script выберите функцию `initializeSheets`
  2. Нажмите ▶️ (Run)
  3. Разрешите доступ к таблице

- [ ] **Web App развёртывание:**
  - [ ] Доступ установлен на "Anyone" или "Anyone with Google account"
  - [ ] URL развёртывания совпадает с `CONFIG.GAS_API_URL` в `app.js` ✅ (уже обновлён)

- [ ] **Пользователи в таблице:**
  - [ ] В листе "Users" добавлен хотя бы один пользователь с вашим email
  - [ ] Формат: `id | name | email | role`
  - [ ] Email должен точно совпадать с email вашего Google аккаунта

### Google Cloud Console (OAuth)
- [ ] **Authorized JavaScript origins:**
  - [ ] Добавлен: `https://heyheylalaley.github.io`
  - [ ] Для локальной разработки: `http://localhost:8000` (опционально)

- [ ] **OAuth consent screen:**
  - [ ] Настроен и опубликован (если требуется)

## 🔍 Диагностика ошибки "NetworkError"

Эта ошибка обычно возникает из-за:

1. **CORS проблемы** - Web App не настроен правильно
   - Решение: Переразверните Web App с доступом "Anyone"

2. **SPREADSHEET_ID не настроен** - самая частая причина
   - Решение: Установите SPREADSHEET_ID в Google Apps Script

3. **Таблицы не созданы** - листы Users/Logs/Settings отсутствуют
   - Решение: Запустите `initializeSheets()` в GAS

4. **Нет прав доступа** - скрипт не может открыть таблицу
   - Решение: Проверьте права доступа к таблице

## 📝 Быстрая проверка

Выполните в консоли браузера (F12):
```javascript
fetch('https://script.google.com/macros/s/AKfycbwPN7R5be2PX35bbtPT8800UbkaYVo86UVJF9v_2qI2xUZrw1vMOCCWyedXB7L7jUFY/exec?action=getSettings')
  .then(r => r.text())
  .then(console.log)
  .catch(console.error)
```

Если видите JSON с `{"success":true,"settings":{...}}` - API работает!
Если видите ошибку - проблема в Google Apps Script.

## ✅ Следующие шаги

1. Откройте Google Apps Script проект
2. Установите `SPREADSHEET_ID`
3. Запустите `initializeSheets()` для создания структуры
4. Добавьте пользователя в лист "Users"
5. Переразверните Web App (если нужно)
6. Обновите страницу и попробуйте снова
