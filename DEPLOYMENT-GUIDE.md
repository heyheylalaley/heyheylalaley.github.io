# 📖 Подробное руководство по деплою на GitHub Pages

## 🎯 Вариант 1: Автоматический деплой (Рекомендуется) ⭐

Этот способ автоматически деплоит ваше приложение при каждом push в репозиторий.

### Шаг 1: Включите GitHub Pages

1. Откройте ваш репозиторий на GitHub: `https://github.com/heyheylalaley/heyheylalaley.github.io`
2. Нажмите на вкладку **Settings** (вверху справа)
3. В левом меню найдите и нажмите **Pages**
4. В разделе **Build and deployment**:
   - **Source**: выберите **GitHub Actions** (не "Deploy from a branch")
5. Сохраните изменения (если нужно)

### Шаг 2: Файл workflow уже создан

Файл `.github/workflows/deploy.yml` уже создан в проекте. Он автоматически:
- Установит Node.js
- Установит зависимости (`npm ci`)
- Соберет проект (`npm run build`)
- Задеплоит на GitHub Pages

### Шаг 3: Запушьте изменения

```bash
# Добавьте все файлы (включая .github/workflows/deploy.yml)
git add .

# Создайте коммит
git commit -m "Add GitHub Actions deployment"

# Запушьте в ветку test (или main)
git push origin test
```

### Шаг 4: Проверьте деплой

1. Перейдите на вкладку **Actions** в вашем репозитории на GitHub
2. Вы увидите процесс сборки и деплоя
3. Дождитесь завершения (обычно 1-2 минуты)
4. Если все успешно, сайт будет доступен по адресу:
   - `https://heyheylalaley.github.io`

### Шаг 5: Проверьте сайт

Откройте в браузере: `https://heyheylalaley.github.io`

---

## 🔧 Вариант 2: Ручной деплой

Если хотите деплоить вручную (не рекомендуется, т.к. нужно делать каждый раз):

### Шаг 1: Соберите проект

```bash
# Установите зависимости (если еще не установлены)
npm install

# Соберите проект
npm run build
```

После этого в папке `dist/` будет готовый к деплою код.

### Шаг 2: Настройте GitHub Pages

1. Откройте **Settings** → **Pages** в вашем репозитории
2. В разделе **Source**:
   - Выберите ветку (например, `main` или `test`)
   - В поле **Folder** выберите `/ (root)` 
   - ⚠️ **НЕ выбирайте `dist/`** - это не сработает для ручного деплоя
3. Сохраните

### Шаг 3: Создайте ветку gh-pages

```bash
# Создайте новую ветку без истории
git checkout --orphan gh-pages

# Удалите все файлы из индекса
git rm -rf .

# Скопируйте содержимое dist в корень
# Windows PowerShell:
Copy-Item -Path dist\* -Destination . -Recurse -Force

# Linux/Mac:
cp -r dist/* .

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Deploy to GitHub Pages"

# Запушьте ветку
git push origin gh-pages
```

### Шаг 4: Обновите настройки Pages

1. Вернитесь в **Settings** → **Pages**
2. Измените **Source** на ветку `gh-pages`
3. **Folder**: `/ (root)`
4. Сохраните

### Шаг 5: Вернитесь в основную ветку

```bash
git checkout main  # или test
```

---

## ❓ Часто задаваемые вопросы

### Q: Как часто обновляется сайт при автоматическом деплое?

**A:** Автоматически при каждом push в ветки `main` или `test`. Процесс занимает 1-2 минуты.

### Q: Где я могу увидеть статус деплоя?

**A:** Во вкладке **Actions** в вашем репозитории на GitHub.

### Q: Что делать, если деплой не работает?

**A:** 
1. Проверьте вкладку **Actions** - там будут ошибки
2. Убедитесь, что в **Settings** → **Pages** выбран **GitHub Actions** как источник
3. Проверьте, что файл `.github/workflows/deploy.yml` существует в репозитории

### Q: Можно ли деплоить только из определенной ветки?

**A:** Да, отредактируйте файл `.github/workflows/deploy.yml` и измените:
```yaml
on:
  push:
    branches:
      - main  # Только main
```

### Q: Как откатить деплой?

**A:** Просто сделайте новый push с исправлениями, или откройте **Actions** → выберите неудачный workflow → **Re-run jobs**.

---

## 🎉 Готово!

После успешного деплоя ваш сайт будет доступен по адресу:
**https://heyheylalaley.github.io**

Все изменения будут автоматически деплоиться при каждом push (если используете GitHub Actions).

